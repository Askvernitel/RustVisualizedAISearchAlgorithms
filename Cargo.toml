[package]
name = "RustVisualizedAISearchAlgorithms"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
